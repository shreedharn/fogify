<%= form_for(@fb_logout) do |f| %>
  <% if @fb_logout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fb_logout.errors.count, "error") %> prohibited this fb_logout from being saved:</h2>

      <ul>
      <% @fb_logout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
